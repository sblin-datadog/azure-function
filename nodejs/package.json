{
  "name": "azure-fn-nodejs",
  "version": "1.0.0",
  "description": "Azure Function instrumented with Datadog APM (Node.js)",
  "main": "src/functions/timerTrigger.js",
  "scripts": {
    "start": "func start",
    "build": "echo 'No build step required for plain JS'"
  },
  "dependencies": {
    "@azure/functions": "^4.5.0",
    "@datadog/serverless-compat": "^0.3.0",
    "dd-trace": "^5.30.0"
  },
  "engines": {
    "node": ">=20"
  }
}
